apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'


dependencies {
    androidTestImplementation(TestDi.hiltTesting)
    androidTestImplementation(TestDi.androidCoreTesting)
    androidTestImplementation(TestDi.fragmentTest)
    androidTestImplementation(TestDi.junit4)
    androidTestImplementation(TestDi.coroutinesTest)

    testImplementation(EspressoTesting.JUNIT)
    androidTestImplementation(EspressoTesting.ESPRESSO)
    androidTestImplementation(EspressoTesting.TEST_RUNNER)


    testImplementation(TestDi.hiltTesting)
    testImplementation(TestDi.androidCoreTesting)
    testImplementation(TestDi.fragmentTest)
    testImplementation(TestDi.junit4)
    testImplementation(TestDi.coroutinesTest)
}
