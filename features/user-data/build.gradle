apply from: "$rootDir/${CommonBuild.FeatureModule}"

dependencies {
    api project(Navigation.HOME)
    api project(Feature.ADD_PAYMENT)

    implementation(Libs.gligarPicker)
}

android {
    namespace 'com.englizya.user_data'
}
