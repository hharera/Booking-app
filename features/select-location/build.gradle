apply from: "$rootDir/${CommonBuild.FeatureModule}"

dependencies {

    api project(Base.COMMON)
    api(Libs.playServicesMaps)
    api(Libs.googlePlaces)
}

android {
    namespace 'com.englizya.select_location'
}