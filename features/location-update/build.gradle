apply from: "$rootDir/${CommonBuild.FeatureModule}"

dependencies {
    implementation Libs.playServicesMaps
    implementation Ktor.ktorClient
    implementation Ktor.ktorClientAndroid
    api project(Data.REMOTE_SOCKET)
}

android {
    namespace 'com.englizya.location_update'
}