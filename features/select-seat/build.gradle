apply from: "$rootDir/${CommonBuild.FeatureModule}"

dependencies {
    api project(UiComponents.BOOKING)
    implementation 'com.github.murgupluoglu:seatview-android:2.0.0'
}

android {
    namespace 'com.englizya.select_seat'
}