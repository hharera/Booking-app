apply from: "$rootDir/${CommonBuild.FeatureModule}"

dependencies {
    api project(UiComponents.BOOKING)
    api project(Core.PAY_TABS)
    api project(Navigation.USER_TICKETS)
}

android {
    namespace 'com.englyzia.booking_payment'
}